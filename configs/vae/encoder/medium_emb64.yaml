model:
  params:
    autoencoder_config:
      model_size: medium
      params:
        encoder_config:
          params:
            in_ch_proj_dim: 64
            conv1d_channels: [64, 64, 128, 128, 256, 256]